
<% if @feed_items.any? %>
  <ol class="microposts">
  <!--@feed_itemsの実態はモデルのインスタンスオブジェクトの集合（ここでは@micropost）、よって_micropost.html.erbを要素分だけ表示する-->
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>
